import test from "node:test";import assert from "node:assert/strict";import { PUBLIC_PRODUCTS } from "../../config/publicSiteContent.js";test("public roadmap exposes only verified product as available",()=>{const available=Object.values(PUBLIC_PRODUCTS).filter(x=>x.status==="Available");assert.deepEqual(available.map(x=>x.name),["MITRA NIDHI CHITI PRO"]);assert.ok(Object.values(PUBLIC_PRODUCTS).filter(x=>x.status!=="Available").every(x=>x.steps.length===0))});
