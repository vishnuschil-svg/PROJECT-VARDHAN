import test from "node:test";import assert from "node:assert/strict";import { createSupportTicket,replyToTicket,updateTicket } from "../../services/supportTicketService.js";const context={tenant_id:"tenant-support",data_scope:"tenant_chit_operations"};
test("support tickets capture safe context, replies and timeline",()=>{const store=new Map();global.window={localStorage:{getItem:k=>store.get(k)||null,setItem:(k,v)=>store.set(k,v)}};const ticket=createSupportTicket({subject:"Duplicate payment blocked",description:"Cannot post July",module:"COLLECTIONS",route:"/chits/collections"},context);assert.equal(ticket.tenant_id,"tenant-support");assert.ok(ticket.safeErrorCode);assert.equal(ticket.status,"Open");const replied=replyToTicket(ticket,"Please review",context);assert.equal(replied.replies.length,1);const closed=updateTicket(replied,{status:"Resolved",note:"Existing receipt found"},context);assert.equal(closed.history.at(-1).status,"Resolved");delete global.window});
