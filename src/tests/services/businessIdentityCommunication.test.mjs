import test from "node:test";import assert from "node:assert/strict";import { saveBusinessIdentity,getBusinessIdentity } from "../../services/businessIdentityService.js";import { createMessageJob,validateTemplate } from "../../services/communicationService.js";const context={tenant_id:"identity-a",data_scope:"tenant_chit_operations"};
test("business identity remains tenant scoped",()=>{const store=new Map();global.window={localStorage:{getItem:k=>store.get(k)||null,setItem:(k,v)=>store.set(k,v)}};saveBusinessIdentity({businessName:"ABC Chits",primaryColor:"#000000"},context);assert.equal(getBusinessIdentity(context).businessName,"ABC Chits");assert.equal(getBusinessIdentity({tenant_id:"identity-b",data_scope:"tenant_chit_operations"}).businessName,"");delete global.window});
test("provider jobs never claim readiness when adapter is unavailable",()=>{const store=new Map();global.window={localStorage:{getItem:k=>store.get(k)||null,setItem:(k,v)=>store.set(k,v)}};const result=createMessageJob({dedupeKey:"email-1",channel:"EMAIL",body:"Hello"},context);assert.equal(result.job.status,"PROVIDER_NOT_CONFIGURED");delete global.window});
test("message template variables are validated",()=>{assert.equal(validateTemplate("Hi {{member}} from {{business}}").valid,true);assert.deepEqual(validateTemplate("Hi {{unknown}}").unsupported,["unknown"])});
