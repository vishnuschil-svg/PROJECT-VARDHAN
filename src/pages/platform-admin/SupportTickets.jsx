import { useMemo,useState } from "react";import AdminLayout from "../../components/platform-admin/AdminLayout";import Table from "../../components/common/Table";import Badge from "../../components/common/Badge";import Modal from "../../components/common/Modal";import { SupportTicketRepository } from "../../repositories/SupportTicketRepository";import { updateTicket } from "../../services/supportTicketService";
export default function SupportTickets(){const [version,setVersion]=useState(0);const [selected,setSelected]=useState(null);const tickets=useMemo(()=>SupportTicketRepository.listAll(),[version]);const columns=[{key:"id",label:"Ticket"},{key:"tenant_id",label:"Tenant"},{key:"subject",label:"Subject"},{key:"priority",label:"Priority"},{key:"status",label:"Status",render:value=><Badge label={value} variant={value==="Closed"?"success":"warning"}/>},{key:"safeErrorCode",label:"Safe code"}];function setStatus(status){const context={tenant_id:selected.tenant_id,data_scope:selected.data_scope};const updated=updateTicket(selected,{status,note:`Support changed status to ${status}`},context);setSelected(updated);setVersion(x=>x+1)}return <AdminLayout title="Support Tickets" subtitle="Tenant-safe support diagnostics and status history"><Table columns={columns} data={tickets} onRowClick={setSelected}/><Modal isOpen={Boolean(selected)} onClose={()=>setSelected(null)} title={selected?.subject}>{selected&&<div><p><strong>User summary:</strong> {selected.userSummary}</p><p><strong>Technical summary:</strong> {selected.technicalSummary}</p><pre>{JSON.stringify(selected.diagnostics,null,2)}</pre><label>Status<select value={selected.status} onChange={e=>setStatus(e.target.value)}>{["Open","AI Reviewing","Waiting for User","Assigned","In Progress","Resolved","Closed"].map(x=><option key={x}>{x}</option>)}</select></label></div>}</Modal></AdminLayout>}
