# Lucky Draw Engine

Lucky draw supports deterministic test mode and repository-backed confirmed results.

Rules:

- lifted/locked members excluded
- duplicate month winner blocked
- inactive group blocked
- closed month support is modeled for validation

Animation remains UI-side only; winner confirmation is service-driven.
