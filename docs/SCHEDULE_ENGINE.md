# Schedule Engine

`ScheduleEngine` supports:

- Legacy fixed group adapter
- Auto-generate month rows
- Copy value to all months
- Copy previous month values
- Apply values to month range
- Percentage increase/decrease
- Mark special/company month

Legacy groups are not mutated. They are adapted into confirmed fixed schedule rows at runtime or when explicitly saved.
