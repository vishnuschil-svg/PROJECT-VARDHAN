# Organizer Memory

Organizer memory is structured local/demo memory, not model training.

Chit Studio includes a preference panel that can seed/apply confirmed preferences. Tenant isolation is enforced by scoped storage.
