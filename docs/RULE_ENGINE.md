# Rule Engine

`RuleEngine` supports:

- Default configurable rule set
- Fixed and percentage commission resolution
- Min/max bid validation
- Winner lock checks

Default lift rule is `NEXT_MONTH`. Default winner lock rule is `ONCE_LIFTED_LOCKED`.
