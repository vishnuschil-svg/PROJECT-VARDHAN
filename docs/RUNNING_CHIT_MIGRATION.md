# Running Chit Migration

Architecture supports migration metadata and schedule-driven opening balances.

Version 1.0 target migration modes:

- Full History Migration
- Opening Balance Migration
- Current Month Only with opening balances

Historical rows must be marked `MIGRATED` and source references preserved.
